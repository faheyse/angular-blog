image: node:16.17.0
pages:
  cache:
    paths:
      - node_modules/
  script:
    - npm install -g @angular/cli@14.2.8
    - npm install
    - npm run build
  artifacts:
    paths:
      - public
  only:
    - main
