image: node:latest
pages:
  stage: deploy
  script:
  - npm install -g @angular/cli
  - npm install
  - ng build
  - mkdir .public
  - cp -r * .public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - main
